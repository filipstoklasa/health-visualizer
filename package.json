{
  "name": "health-visualizer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A local-first dashboard for your Apple Health export.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test test.mjs",
    "test:browser": "node --test browser-test.mjs"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.12",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
